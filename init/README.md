# init

PID 1. Empty until ROADMAP M6.

Design intent inherited from the Galdr experiment: no_std-style discipline in
C++ terms - no heap at first light, explicit phases, every action it takes
must itself be authorized by a policy blob supplied by whoever booted us.
