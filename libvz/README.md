# libvz

Freestanding foundation library. Bottom of the dependency chain: nothing in
this repository may depend on anything outside `include/` here, and this
component depends on no sibling.

Contents grow only when two or more components need the same thing.
